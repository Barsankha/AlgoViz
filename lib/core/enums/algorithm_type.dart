enum AppTheme { light, dark, syetem }

enum SettingMode { visuals, motion, about }

enum AlgoCategory {
  sorting,
  searching,
  graph,
  tree,
  binaryTree,
  linkedList,
  stack,
  queue,
  hashmap,
  hashset,
}

//
enum ArrayAlgorithmType {
  bubbleSort,
  selectionSort,
  insertionSort,
  quickSort,
  mergeSort,
  heapSort,
  countingSort,
  redixSort,
  bucketSort,
  shellSort,
  cocktailSort,
  cycleSort,
  introSort,
  timSort,
  treeSort,
}

enum ArraySortAlgoType {
  linearSearch,
  binarySearch,
  jumpSearch,
  interpolationSearch,
  exponentialSearch,
  fibonacciSearch,
  ternarySearch,
}

enum GraphAlgoType {
  breathFirstSearch,
  depthFirstSearch,
  dijkstra,
  prims,
  kruskal,
  bellManFord,
  aStar,
  floydWarshall,
  topologicalSort,
  tarjan,
  kahns,
  kosaraju,
}

enum TreeAlgoType { bfs, dfsPre, dfsPost }

enum BinaryTreeAlgoType { dfsIn }

enum LinkedListAlgoType { traverse, search, insert, delete, reverse }

enum StructureType { stack, queue }

enum DsType { hashMap, hashSet }
